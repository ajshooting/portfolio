---
import Layout from '../../layouts/Layout.astro';
// import projects from '../../data/projects.json';

// export async function getStaticPaths() {
//   return projects.map((project: { url: string; }) => ({
//     params: { project: project.url.split('/').pop() },
//     props: { project },
//   }));
// }

const { project } = Astro.props;
---

<Layout>
  <main>
    <h1>{project.title}</h1>
    <p>{project.description}</p>
    <ul>
      {project.tags.map((tag: unknown) => <li>{tag}</li>)}
    </ul>
    <!-- その他のコンテンツ -->
  </main>
</Layout>